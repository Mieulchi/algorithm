#include <iostream>
#include <algorithm>
using namespace std;

int a, b;

string arr[50];

void solve() {
	cout << (a ^ b);
}

int main() {
	cin.tie(NULL);
	cin.sync_with_stdio(false);

	cin >> a >> b;

	solve();
}