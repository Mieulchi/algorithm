#include <stdio.h>

int main() {
	int n;
	scanf("%d", &n);

    int x;
    scanf("%d", &x);
    n-=x;
    scanf("%d", &x);
    n-=x;
    scanf("%d", &x);
    n-=x;
    scanf("%d", &x);
    n-=x;
    scanf("%d", &x);
    n-=x;
    scanf("%d", &x);
    n-=x;
    scanf("%d", &x);
    n-=x;
    scanf("%d", &x);
    n-=x;
    scanf("%d", &x);
    n-=x;
    
    printf("%d", n);
}